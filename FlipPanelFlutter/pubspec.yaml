name: flip_panel
description: Z Flip 4 悬停态桌面端 PC 媒体陪伴控制台
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.2
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  wakelock_plus: ^1.2.8
  battery_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
